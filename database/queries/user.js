const user = {
    // list: `SELECT * FROM questions `,
    // admin:(data)=>{

    //     return `SELECT * FROM pros WHERE pros.username =${data}`
    // }
    admin: `SELECT * FROM pros WHERE username ="javis"   `
}
    module.exports = user;